# ============================================
# MCP Framework - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ---------- Flask ----------
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-this-in-production
HOST=0.0.0.0
PORT=5000

# ---------- JWT Auth ----------
JWT_SECRET_KEY=your-jwt-secret-key
JWT_EXPIRES_HOURS=24

# ---------- CORS ----------
# Comma-separated list of allowed origins
CORS_ORIGINS=*

# ---------- AI APIs ----------
# OpenAI (required for content generation)
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic Claude (fallback)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Default model to use
DEFAULT_AI_MODEL=gpt-4o-mini
FALLBACK_AI_MODEL=claude-3-sonnet-20240229

# ---------- SEO Tools ----------
# SEMrush API (get from semrush.com subscription info)
SEMRUSH_API_KEY=your-semrush-api-key

# Ahrefs API (optional)
AHREFS_API_KEY=your-ahrefs-api-key

# ---------- WordPress ----------
WP_BASE_URL=https://your-wordpress-site.com
WP_USERNAME=your-wp-username
WP_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx-xxxx

# ---------- Google Business Profile ----------
# OAuth credentials (get from Google Cloud Console)
GBP_CLIENT_ID=your-gbp-client-id.apps.googleusercontent.com
GBP_CLIENT_SECRET=your-gbp-client-secret
# Per-client refresh tokens are stored in database

# ---------- Google Analytics 4 ----------
GA4_PROPERTY_ID=123456789
# JSON string of service account credentials
GA4_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# ---------- Social Media ----------
# Facebook
FACEBOOK_ACCESS_TOKEN=your-facebook-page-access-token
FACEBOOK_PAGE_ID=your-facebook-page-id

# Instagram (uses Facebook Graph API)
INSTAGRAM_ACCESS_TOKEN=your-instagram-access-token

# LinkedIn
LINKEDIN_ACCESS_TOKEN=your-linkedin-access-token

# ---------- Data Storage ----------
DATA_DIR=./data

# ---------- Content Defaults ----------
DEFAULT_BLOG_WORD_COUNT=1200
DEFAULT_TONE=professional

# ---------- Rate Limiting ----------
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# ---------- Background Jobs ----------
# Set to 'false' to disable automatic scheduling
ENABLE_SCHEDULER=true

# ---------- Email Notifications ----------
# SendGrid (recommended)
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# OR use SMTP (fallback)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email settings
FROM_EMAIL=noreply@youragency.com
EMAIL_FROM_NAME=Karma Marketing

# ---------- SMS Notifications ----------
# Twilio (for instant lead alerts)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_FROM_NUMBER=+19415551234

# ---------- CallRail Integration ----------
# Call tracking, recordings, and transcripts
# Get from CallRail Settings > API > API V3
CALLRAIL_API_KEY=your-callrail-api-key
CALLRAIL_ACCOUNT_ID=your-account-id

# ---------- Webhooks / N8N Integration ----------
# URLs where MCP fires events (N8N, Zapier, etc.)
# Set one or more based on your workflow setup
WEBHOOK_URL_DEFAULT=https://your-n8n.com/webhook/mcp-events
WEBHOOK_URL_CONTENT=https://your-n8n.com/webhook/content
WEBHOOK_URL_LEADS=https://your-n8n.com/webhook/leads
WEBHOOK_URL_CALLS=https://your-n8n.com/webhook/calls
# HMAC secret for webhook signatures (optional but recommended)
WEBHOOK_SECRET=your-webhook-signing-secret

# App URL (for email links)
APP_URL=https://mcp-framework.onrender.com

# ===========================================
# SFTP STORAGE (for persistent file uploads)
# ===========================================
# Configure these to store uploads on your own server/hosting
# SFTP_HOST=ftp.yourdomain.com
# SFTP_PORT=22
# SFTP_USERNAME=your_ftp_username
# SFTP_PASSWORD=your_ftp_password
# SFTP_REMOTE_PATH=/public_html/uploads
# SFTP_BASE_URL=https://yourdomain.com/uploads
